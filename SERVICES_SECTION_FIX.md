# Исправление секции "Услуги и цены"

## ❌ Проблема
В секции "Услуги и цены":
- Заголовок "Услуги и цены" был белым (должен быть белым на темном фоне)
- Кнопка "Узнать точную стоимость" была темной (должна быть белой)

## ✅ Исправления

### 🎯 Заголовок секции
**Было:** Градиентный текст (из общих стилей)
**Стало:** Белый текст с тенью

```css
.services-section .section-title {
  color: white !important;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  background: none !important;
  -webkit-background-clip: unset !important;
  -webkit-text-fill-color: white !important;
  background-clip: unset !important;
  filter: none !important;
}
```

### 🔘 Кнопка "Узнать точную стоимость"
**Было:** Темная кнопка
**Стало:** Белая кнопка с темным текстом

```css
.services-section .btn {
  background-color: white !important;
  color: var(--primary-color) !important;
  font-weight: 600;
  border: 2px solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2);
}

.services-section .btn:hover {
  background-color: rgba(255, 255, 255, 0.95) !important;
  color: var(--darker-red) !important;
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
}
```

### 🌈 Улучшенный фон секции
**Обновлен градиент:**
```css
.services-section {
  background: linear-gradient(135deg, 
    var(--darker-red) 0%, 
    var(--primary-color) 25%, 
    var(--accent-color) 75%, 
    var(--light-red) 100%);
}
```

### ✨ Подчеркивание заголовка
**Белое подчеркивание:**
```css
.services-section .section-title::after {
  background: linear-gradient(90deg, 
    rgba(255, 255, 255, 0.8), 
    rgba(255, 255, 255, 0.6), 
    rgba(255, 255, 255, 0.8)) !important;
  box-shadow: 0 2px 8px rgba(255, 255, 255, 0.3) !important;
}
```

## 🎯 Результат

Теперь в секции "Услуги и цены":
- ✅ Заголовок белый с тенью
- ✅ Кнопка белая с темным текстом
- ✅ Подчеркивание белое
- ✅ Отличная читаемость на темном фоне
- ✅ Единый стиль с остальным сайтом

## 📝 Изменения в файлах
- `css/style.css` - обновлены стили секции услуг
- `index.html` - убран лишний класс `services-title`